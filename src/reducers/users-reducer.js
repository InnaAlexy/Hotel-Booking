const initialUsersState = {
	// id: null,
	// login: null,
	// roleId: ,
	// session: null,
};

export const usersReducer = (state = initialUsersState, action) => {
	switch (action.type) {
		default:
			return state;
	}
};
