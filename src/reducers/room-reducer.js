const initialRoomState = {
	// id: null,
	// login: null,
	// roleId: ,
	// session: null,
};

export const roomReducer = (state = initialRoomState, action) => {
	switch (action.type) {
		default:
			return state;
	}
};
