const initialRoomsState = {
	// id: null,
	// login: null,
	// roleId: ,
	// session: null,
};

export const roomsReducer = (state = initialRoomsState, action) => {
	switch (action.type) {
		default:
			return state;
	}
};
